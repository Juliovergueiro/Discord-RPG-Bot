I see the issue now. The Math.max() function expects at least two arguments to work properly. Since you're only providing one argument (0) in this case, it won't behave as expected.

To fix this, you can pass two arguments to Math.max(). You can do this by passing the desired value as the first argument and '0' as the second argument like this:

let olar = 10 - 13 + Math.max(0, 0);
console.log(olar);
Now, when you run this code, the value of 'olar' should be correctly calculated as 0, and you should see '0' printed in the console.

I understand the issue now. The problem is that the subtraction operation (10 - 13) is being calculated before applying the Math.max(0, 0). Since 10 - 13 results in -3, the Math.max(0, 0) is not affecting the final result.

To ensure that the Math.max(0, 0) adjustment is incorporated into the final value before subtracting 13, you can use parentheses to control the order of operations. Here's how you can adjust the code:

let olar = (10 - 13) + Math.max(0, 0);
console.log(olar);
By enclosing (10 - 13) in parentheses, you are ensuring that the Math.max(0, 0) is applied to the result of 10 - 13 before the subtraction takes place. This should give you the desired result of 0.

let Oie = "1, 2"

// console.log(Oie.charAt(3))
// console.log(Oie.slice(3))
// console.log(Oie.length) (pra ver o tamanho dela)
// console.log(Oie.indexOf("1"))

let SlicedOie = Oie.substring(Oie.indexOf("1"), 4)

// console.log(SlicedOie)

// indexOf = vai achar a palavra Acelerar na string e retornar onde que tá o "A" de Acelerar, ou seja, a primeira letra

let CortarAce = Atacante.Efeitos.substring(Atacante.Efeitos.indexOf("Acelerar"), 13)

let NumeroAcelerar = CortarAce.slice(10, 12)
NumeroAcelerar = parseInt(NumeroAcelerar)

IniciativaAtk += NumeroAcelerar

console.log(CortarAce)

console.log(parseInt(NumeroAcelerar))

// const AcelerarReplace = Atacante.Efeitos.replace(`${1}`, "")

if(inicio_comando == "REVEST"){

    let id_member = msg.member.id
    let char = personagem_logado(id_member)

if(char.Efeitos.includes("Revestimento Comum")){

const Newefeitos = char.Efeitos.replace("Revestimento Comum", "")

char.Efeitos = Newefeitos

msg.channel.send(`**${char.Nome}** desativou **Revestimento Comum*.`)
db.write()

let FlavorRead = techs.get("Techs").find({Nome : "FlavorRead" }).value()

FlavorRead.BF = "N"

const FaiscasReplace = char.Efeitos.replace("Abençoado pelas Faiscas - ", "")

char.Efeitos = FaiscasReplace

} else {

char.Efeitos += "Revestimento Comum"

db.write()

msg.channel.send(`**${char.Nome}** ativou **Revestimento Comum**.`)}

}

if(meiocomando == "Revcomum" && !char.Efeitos.includes("Revestimento Comum")){

let ZeroOuNo = ``

if(Math.floor(char.MP*0.02) < 10){

ZeroOuNo = `0`

}

let Revest = `${ZeroOuNo}${Math.floor(char.MP*0.02)}`

char.Efeitos += `Revestimento Comum [${Revest}]. `

} else if (char.Efeitos.includes("Revestimento Comum")){

let CortarAce = char.Efeitos.substring(char.Efeitos.indexOf("Revestimento Comum"), 24)

let NumeroRevest = CortarAce.slice(20, 22)
NumeroRevest = parseInt(NumeroRevest)

let RevestReplace = char.Efeitos.replace(`Revestimento Comum [${NumeroRevest}]. `, ``)

char.Efeitos += RevestReplace

console.log(NumeroRevest)

}

if(meiocomando == "Revestimento Avançado"){

let Revest = 1 + Math.floor(Defensor.MP*0.03)

char.MP_Ativacao += Revest

char.Efeitos += `Revestimento Avançado [${Revest}]. `

} else if (char.Efeitos.includes("Revestimento Avançado")){

}

if(meiocomando == "Revestimento Excessivo"){

let Revest = 5 + Math.floor(Defensor.MP*0.05)

char.MP_Ativacao += Revest

char.Efeitos += `Revestimento Excessivo [${Revest}]. `

} else if (char.Efeitos.includes("Revestimento Excessivo")){

}








let oie = "Em Movimento [9]"

let ola = oie.slice(14, 16)

ola = parseInt(ola)

if(ola <= 9){

ola = oie.slice(14,15)

}

console.log(ola)

let oie = "Em Movimento [9]"

let ola = oie.slice(14, 16)

ola = parseInt(ola)

if(ola <= 9){

ola = oie.slice(14,15)

}

ola = parseInt(ola)

console.log(ola)

let EfeitoMov = "Em Movimento [11, 17]. "

let CortarMov2 = EfeitoMov.slice(18, 20)

let CortarMov1 = EfeitoMov.slice(14, 16)

console.log(CortarMov1)